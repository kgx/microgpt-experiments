# MicroGPT experiments

```bash
python3 train.py              # train, saves model.json
python3 main.py               # generate (no prompt)
python3 main.py -i "Li"       # generate from prompt (use quotes if spaces)
```